<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= applicationName %></title>
  <%- include('./partials/scripts'); %>
  <style>
    .landing-page {
      min-height: 100vh;
      background: linear-gradient(135deg, #007bff 0%, #6610f2 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }
    .landing-content {
      background: white;
      border-radius: 15px;
      box-shadow: 0 0 30px rgba(0,0,0,0.1);
      padding: 3rem;
      text-align: center;
      max-width: 800px;
      width: 100%;
    }
    .logo-container {
      margin-bottom: 2rem;
    }
    .logo-container img {
      max-width: 300px;
      height: auto;
    }
    .welcome-text {
      color: #333;
      margin-bottom: 2rem;
    }
    .welcome-text h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: #007bff;
    }
    .welcome-text p {
      font-size: 1.2rem;
      color: #666;
    }
    .action-buttons {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }
    .action-buttons .btn {
      padding: 0.8rem 2rem;
      font-size: 1.1rem;
      border-radius: 50px;
      transition: all 0.3s ease;
    }
    .action-buttons .btn-primary {
      background: #007bff;
      border-color: #007bff;
    }
    .action-buttons .btn-outline-primary {
      color: #007bff;
      border-color: #007bff;
    }
    .action-buttons .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    }
    @media (max-width: 768px) {
      .landing-content {
        padding: 2rem;
      }
      .welcome-text h1 {
        font-size: 2rem;
      }
      .welcome-text p {
        font-size: 1rem;
      }
      .action-buttons {
        flex-direction: column;
      }
      .action-buttons .btn {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="landing-page">
    <div class="landing-content">
      <div class="logo-container">
        <img src="<%= BASE_URL+softwareLogo %>" alt="<%= applicationName %> Logo" class="img-fluid">
      </div>
      <div class="welcome-text">
        <h1>Welcome to <%= applicationName %></h1>
        <p>Streamline your invoicing process with our powerful and easy-to-use invoice management system.</p>
      </div>
      <div class="action-buttons">
        <a href="/login" class="btn btn-primary" target="_blank">Login</a>
      </div>
    </div>
  </div>
</body>
</html>
